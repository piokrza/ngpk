{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "add-lint-changed",
  "type": "object",
  "description": "Adds a lint-changed script which runs ESLint only on files that have changed in respect to the default git branch.",
  "properties": {
    "name": {
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
