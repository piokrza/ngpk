{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "generate-angular-library",
  "type": "object",
  "description": "Generate a new Angular library.",
  "properties": {
    "name": {
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "directory": {
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 1
      }
    }
  },
  "required": ["name"]
}
